defmodule Turbo.EctoTest do
  use ExUnit.Case
  doctest Turbo.Ecto

  test "hello 2021" do
    # post = Turbo.Ecto.TestFactory.insert(:post, id: 1)
    # IO.inspect(product, label: "product")
    # assert post.id == 1
  end
end
